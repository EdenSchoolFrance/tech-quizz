{
  "name": "tech-quiz",
  "version": "1.0.0",
  "description": "Tech Quiz Application",
  "scripts": {
    "front": "git pull && cd Front && pnpm i && pnpm run dev",
    "back": "git pull && cd Back && composer i && php artisan serve"
  }
} 